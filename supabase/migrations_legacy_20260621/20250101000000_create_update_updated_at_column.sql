-- Bootstrap helper used by multiple historical migrations.
-- Keep this early so triggers can safely reference it during fresh db push.

CREATE OR REPLACE FUNCTION public.update_updated_at_column()
RETURNS trigger
LANGUAGE plpgsql
SET search_path TO 'public'
AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$;
